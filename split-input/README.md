This directory contains batches of records sliced from the input files.
It is only used as a temporary location; files will be either removed or
moved to the `slices` directory during an update process.
